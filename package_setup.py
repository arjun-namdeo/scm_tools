#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
Setting up the current package for use
before using the package, You need to run this module so that
It can configure the whole package for your running env
"""
package_paths = dict()

package_paths["PATH"] = ["bin"]
package_paths["PYTHONPATH"] = [""]
